# medguard
MedGuard is a web app that checks medicine safety using an explainable rule-based engine. It scans medicine labels, considers user context (age, conditions, food, current meds), and clearly explains why a medicine is safe, risky, or unsafe. AI is used only for explanations.
